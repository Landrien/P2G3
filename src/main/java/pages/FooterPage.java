package pages;

import org.openqa.selenium.WebDriver;

/**
 * The footer part of any page on the website containing the social media links
 */
public class FooterPage extends BasePage
{
    public FooterPage(WebDriver driver)
    {
        super(driver);
    }
}
